services:
  livekit:
    image: livekit/livekit-server:v1.9.1@sha256:c039a1bfa154c8479ac369c380665638e92a7e9531e69664549c0c0d3eb65e63
    command: --dev --bind 0.0.0.0 --node-ip 127.0.0.1
    ports:
      - "7880:7880"
      - "7881:7881"
      - "7882-7892:7882-7892/udp"

  app:
    build: .
    ports:
      - "3001:3000"
    depends_on:
      - livekit
    environment:
      - VOICE_PROVIDER=nova-sonic
      - APP_ENV=local
      - APP_AUTH_MODE=${APP_AUTH_MODE:-token}
      - APP_API_TOKEN=${APP_API_TOKEN:?set APP_API_TOKEN in your local .env}
      - APP_ROOM_ID=${APP_ROOM_ID:-kanban-meeting}
      - APP_BOARD_ID=${APP_BOARD_ID:-default}
      - BOARD_SQLITE_PATH=${BOARD_SQLITE_PATH:-/srv/data/board.sqlite}
      - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
      - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
      - AWS_SESSION_TOKEN=${AWS_SESSION_TOKEN}
      - AWS_REGION=us-east-1
      - LIVEKIT_URL=ws://livekit:7880
      - LIVEKIT_BROWSER_URL=${LIVEKIT_BROWSER_URL:-ws://localhost:7880}
      - LIVEKIT_API_KEY=${LIVEKIT_API_KEY:-devkey}
      - LIVEKIT_API_SECRET=${LIVEKIT_API_SECRET:-secret}
      - NOVA_SONIC_MODEL=amazon.nova-sonic-v1:0
      - NOVA_SONIC_VOICE=matthew
      - JIRA_CONFIG_PATH=${JIRA_CONFIG_PATH:-}
      - JIRA_CONFIG_JSON=${JIRA_CONFIG_JSON:-}
      - JIRA_API_TOKEN=${JIRA_API_TOKEN:-}
      - JIRA_WEBHOOK_SECRET=${JIRA_WEBHOOK_SECRET:-}
      - AUDIT_LOG_PATH=${AUDIT_LOG_PATH:-}
      - TRUST_PROXY_HEADERS=${TRUST_PROXY_HEADERS:-}
    volumes:
      - ./config:/srv/config:ro
      - board-data:/srv/data

volumes:
  board-data:
