// Command server runs the Auto Bot meeting application.
//
// Most Go identifiers in this package are runtime internals. The practical
// contracts exposed outside the process are the HTTP routes in docs/api,
// WebSocket event names used by the browser clients, and the tool schemas
// returned by the board runtime.
package main
